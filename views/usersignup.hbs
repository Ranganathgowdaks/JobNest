<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup Form</title>
    {{!-- <link rel="stylesheet" type="text/css" href="signup.css"> --}}
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-image: url('/signupbackground.png');
            background-repeat: no-repeat;
            background-position: center center;
            background-attachment: fixed;
            background-size: cover;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .signup {
            width: 400px;
            padding: 20px 40px;
            background: rgba(255, 255, 255, 0.8);
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        h1 {
            margin-bottom: 20px;
            font-size: 24px;
            color: #2c3e50; /* Dark blue color */
        }

        label {
            display: block;
            margin-top: 10px;
            font-size: 14px;
            color: #2c3e50; /* Dark blue color */
            text-align: left;
        }

        input[type="text"],
        input[type="email"],
        input[type="number"],
        input[type="password"] {
            width: calc(100% - 20px);
            height: 40px;
            margin: 10px 0;
            padding-left: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        #signup2 {
            width: 100%;
            height: 40px;
            border: none;
            border-radius: 5px;
            color: #fff;
            background-color: #0D7A5F; /* Orange color */
            margin-top: 20px;
            cursor: pointer;
            font-size: 16px;
        }

        #signup2:hover {
            background-color: #0D7A5F; /* Darker orange color for hover */
        }

        #forgot {
            display: block;
            margin-top: 15px;
            color: #3498db; /* Light blue color */
            text-decoration: none;
            font-size: 14px;
        }

        #forgot:hover {
            color: #2980b9; /* Darker blue color for hover */
        }

        .logo {
            display: block;
            margin: 20px auto;
            height: 100px;
            width: 100px;
            border-radius: 50%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="signup">
        {{!-- <img src="./signuplogo.jpg" class="logo" alt="Logo"> --}}
        <h1>Sign Up</h1>
        <form id="signup" method="POST">
            <label for="Uname"><b>Username</b></label>
            <input type="text" name="name" id="Uname" placeholder="Username" required>
            <label for="Email"><b>Email</b></label>
            <input type="email" name="email" id="Email" placeholder="Email" required>
            <label for="phonenumber"><b>Phone</b></label>
            <input type="number" name="phone" id="phonenumber" placeholder="Mobile" required>
            <label for="Pass"><b>Password</b></label>
            <input type="password" name="password" id="Pass" placeholder="Password" required>
            <input type="submit" name="signup" id="signup2" value="Sign Up Here">
            <a href="#" id="forgot">Forgot Password</a>
        </form>
    </div>
</body>
</html>
